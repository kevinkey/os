#ifndef TYPES_H
#define TYPES_H

#include <stddef.h>
#include <stdint.h>
#include <stdbool.h>

typedef unsigned int uint_t;
typedef int int_t;

#define MAX(a, b) ((a) >= (b) ? (a) : (b))

#define MIN(a, b) ((a) < (b) ? (a) : (b))

#endif